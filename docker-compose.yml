services:
  web:
    build: .
    ports: ["8080:80"]
    restart: unless-stopped
  # Будущий backend (NestJS + PostgreSQL) — раскомментировать при подключении API:
  # api:
  #   build: ./backend
  #   env_file: .env
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: legion
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}